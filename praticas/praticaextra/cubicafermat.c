#include <stdio.h>
#include <math.h>

int main(){
    int  y, z, k;
    long int p;
    float x, n;
    y = 50;
    while(p!=z){
        x = (4*y -3)/12;
        n = sqrt(x) - 1/2;
        k = n;
        z = 3*k^2 + 3*k + 1;
        p = pow(y,3);
        y++;
        printf("%i nao serve ",p);
        }
    printf("N = %f e P = %i\n",n,p);


    return 0;
}